/** Initializes a new Square Dance game and starts running it. */
class Main {
    function void main() {
        var GhostGame game;
        let game = GhostGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
