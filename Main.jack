/** Initializes a new Car Dance game and starts running it. */
class Main {
    function void main() {
        var CarGame game;
        let game = CarGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
